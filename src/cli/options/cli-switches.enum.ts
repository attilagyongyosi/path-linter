/**
 * Enumeration of supported
 * command line switches.
 *
 * @author  attilagyongyosi
 * @see     processCliOptions
 */
export enum CliSwitches {
    CONFIG = '--config',
    COLORIZE = '--colorize'
}
