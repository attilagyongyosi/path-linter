/* istanbul ignore file  // will be checked in integration tests only */

/**
 * Just a little wrapper enum for shell exit codes,
 * conveniently leveraging TypeScript enum indexing.
 *
 * @author  attilagyongyosi
 */
export enum ExitCodes {
    OK,
    ERROR
}
